# ============================================================
# Decode kernel — uncomment below and comment out prefill
# ============================================================
# [solution]
# name = "gdn-decode-cuda-v1"
# definition = "gdn_decode_qk4_v8_d128_k_last"
# author = "MSInfer"
#
# [build]
# language = "cuda"
# entry_point = "kernel.cu::kernel"
# files = ["kernel.cu"]

# ============================================================
# Prefill kernel (default)
# ============================================================
[solution]
name = "gdn-prefill-cuda-v1"
definition = "gdn_prefill_qk4_v8_d128_k_last"
author = "MSInfer"

[build]
language = "cuda"
entry_point = "kernel_prefill.cu::kernel"
files = ["kernel_prefill.cu"]
